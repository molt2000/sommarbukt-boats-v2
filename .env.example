SMTP_HOST=smtps.udag.de
SMTP_PORT=465
SMTP_USER=boats@sommarbukt.com
SMTP_PASS=changeme

# Random long string — paste same value into Vercel env vars and your .env.local
# Generate one with: openssl rand -hex 32
NEXT_PUBLIC_API_TOKEN=changeme
